# CORS Configuration for Gofsen

# Primary CORS configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://myapp.com,https://www.myapp.com

# Alternative name (fallback)
# ALLOWED_ORIGINS=http://localhost:3000,https://myapp.com

# Optional: Custom methods (default: GET,POST,PUT,DELETE,PATCH,OPTIONS)
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS

# Optional: Custom headers (default: Content-Type,Authorization)
CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With,X-API-Key